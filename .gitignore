.venv/
tmp/
.idea/
log.*
processor*/
postProcessing/
[0-9]*/
[0-9]*.[0-9]*/
# ...but initial conditions are input, not output. Without this a case cannot ship its
# own boundary conditions and will not run from a clean clone.
!cases/*/0.orig/
cases/*/constant/polyMesh/
cases/*/run.pid
cases/*/run.status
