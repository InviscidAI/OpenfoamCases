# Agents

Promoted delegable sub-steps live here after they have appeared in three
separate cases or tasks.

Use agents for self-contained steps that are complex, high-variance, or noisy
enough to isolate from an orchestrator workflow. Each agent should define clear
inputs, outputs, constraints, and a completion or pass/fail signal.
